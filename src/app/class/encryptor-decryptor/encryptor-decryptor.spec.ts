import { EncryptorDecryptor } from './encryptor-decryptor';

describe('EncryptorDecryptor', () => {
  it('should create an instance', () => {
    expect(new EncryptorDecryptor()).toBeTruthy();
  });
});
